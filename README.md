# A_Bit_S Blog（中文）

> 🌐 切换语言 / Language: [English](README_EN.md)

## 项目简介
A_Bit_S Blog 是团队的内容发布阵地，基于 Jekyll 构建。`index.html`、`team.md` 等页面与 `_posts/` 下按日期命名的文章，会通过 `_layouts/` 的布局与 `_includes/` 的片段渲染。样式集中在 `assets/css/style.scss`，站点元数据、导航和插件配置写在 `_config.yml`。构建输出位于 `_site/`，该目录由构建生成，不需要手动修改或提交。

## 快速开始
1. 安装 Ruby 3.x、Bundler，并确保系统可用 `node`（供前端工具使用）。
2. 执行 `bundle install` 安装 `Gemfile` 中声明的依赖。
3. 启动开发服务器：
   ```bash
   bundle exec jekyll serve --livereload
   ```
   浏览器访问 `http://127.0.0.1:4000` 进行预览。

## 开发与校验流程
- **内容编辑**：在 `_posts/` 中新增 `YYYY-MM-DD-title.md` 命名的 Markdown 文件，YAML 头部须包含 `layout`、`title` 及模板所需的自定义变量。
- **样式调整**：修改 `assets/css/style.scss`，该文件支持 SCSS 嵌套，Jekyll 构建时会自动编译。
- **配置修改**：变更导航、URL 结构或插件时编辑 `_config.yml`，修改后需重启 `jekyll serve`。
- **质量检查**：提交前运行 `bundle exec jekyll build --trace`，并使用 `bundle exec jekyll doctor` 检查失效链接或缺失资源。

## 内容创作指南
### 新增文章（`_posts/`）
1. **文件命名**：`YYYY-MM-DD-title.md`，日期决定文章排列顺序，`title` 仅含小写字母、数字与中划线（如 `2025-12-01-zero-downtime.md`）。
2. **Front Matter 模板**：
   ```yaml
   ---
   layout: post
   title: "示例文章标题"
   author: allen          # 对应 authors 目录中的标识
   tags: [devops, release]
   excerpt: >
     1~2 句话摘要，便于列表页展示。
   cover: /assets/images/posts/example-cover.png
   ---
   ```
3. **正文写作**：
   - 单行控制在 80~100 字符，易于代码审阅。
   - 图片、附件放在 `assets/`，以绝对路径引用（如 `![图示](/assets/images/posts/foo.png)`）。
   - 引用链接优先使用参考样式：
     ```
     文本[^ref]

     [^ref]: https://example.com
     ```
4. **发布前检查**：`bundle exec jekyll build --trace` 与 `bundle exec jekyll doctor` 均需通过。

### 新增书籍（`_books/` 与 `_chapters/`）
1. **书籍文件**（`_books/<slug>.md`）：
   ```yaml
   ---
   title: Building Scalable Systems
   slug: building-scalable-systems
   description: 一本介绍分布式系统演进的长文
   cover: /assets/images/books/bss.png   # 可选
   order: 1                              # 控制书籍排序
   ---
   ```
   正文可写阅读指南或内容简介。
2. **章节文件**（`_chapters/<book-slug>-NN.md`）：
   ```yaml
   ---
   title: Chapter 1 — System Foundations
   book: building-scalable-systems   # 必须与 slug 一致
   order: 1
   summary: 章节要点摘要（用于目录）
   permalink: /books/building-scalable-systems/system-foundations/
   ---
   ```
   建议包含引言、核心概念、实践清单、参考资料等模块，并通过 `##`/`###` 标题分隔。
3. **导航关联**：书籍页通常自动列出章节，只需保证 `book` 与 `order` 字段正确，必要时更新 `_config.yml` 中的导航链接。

### Markdown 与 Front Matter 规范
- 正文从 `##`（二级标题）开始，避免与 layout 中的 `h1` 冲突。
- 代码块使用三重反引号并标注语言（示例：在代码块起始行写 &#96;&#96;&#96;bash）。
- 列表使用 `-` 或数字编号，子级缩进两个空格，与现有风格保持一致。
- 首次出现的英文专业词可加括号说明，保持双语可读性。
- Front Matter 键名统一使用 snake_case，例如 `reading_time`、`cover_alt`。

## 部署
当前部署目标是 GitHub Pages（`https://wzxzhuxi.github.io/`）。通过 GitHub Actions 或 Pages 分支执行：
```bash
JEKYLL_ENV=production bundle exec jekyll build
```
将 `_site/` 发布到 Pages，勿将 `_site/` 提交到版本库。

## 贡献指南
遵循 `AGENTS.md` 中的分支、测试和 PR 规范。保持小而聚焦的提交，使用祈使句式（如 `Add hero section layout`），视觉改动附上截图或 GIF。提交评审前确认 `jekyll build` 与 `jekyll doctor` 均通过。
